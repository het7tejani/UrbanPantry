import React from 'react';

const HeroSection = () => (
    <section className="hero">
        <div className="hero-overlay">
            <h1 className="hero-title">Elevate Your Everyday</h1>
            <p className="hero-subtitle">Discover curated home and kitchen essentials designed for modern living.</p>
            <button className="button">Shop New Arrivals</button>
        </div>
    </section>
);

export default HeroSection;
